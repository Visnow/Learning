#include "using.h"
//命名空间（名称空间）
//每个名字都需要一个using声明
//::是作用域操作符，其前面就是名称空间 其后面即为名称
//名称对于名称空间，就像文件放在文件夹中 要用就写完整名称
using std::cout;
using std::endl;
using std::string;
//头文件中不能使用using声明

// using namespaces;

int main() {

    return 0;
}